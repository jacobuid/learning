{
  "name": "json-api",
  "version": "1.0.0",
  "description": "Mock Data JSON Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch data.json --port 4201"
  },
  "author": "Jacob King",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.12.0"
  }
}
